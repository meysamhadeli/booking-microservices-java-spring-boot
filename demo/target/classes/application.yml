spring:
  profiles:
    active: dev
  jpa:
    datasource-url: jdbc:postgresql://localhost:5432/spring_demo
    datasource-username: postgres
    datasource-password: postgres
    datasource-driver-class-name: org.postgresql.Driver
    packagesToScan: sample.spring.student
    hibernate-ddl-auto: update
    hibernate-column_ordering_strategy: legacy

#  flyway:
#    enabled: false
#    baseline-on-migrate: true
#    supported-database-check-disabled: true
#    locations: classpath:db/migration

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    exchangeName: student
    exchangeType: topic